package com.sheshu.cci5.arraysstrings;

public class CReverse {

    private void cReverse(){
        /*

        char* reverse(char* in){
        int len = sizeof(in); // expecting 1 extra for null
        if(len<3)
            return in; // nothing to reverse upto 1char with null char.. so <=2 or <3 no need to reverse.

            for(int i=0;i<len/2;i++){
                char temp = in[len-i-2]; // 1 for null, 1 for 0 index.
                in[len-i-2] = in[i];
                in[i] = temp;
            }
        }

        *
         */

    }
}
